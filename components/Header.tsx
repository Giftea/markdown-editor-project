import React from 'react'
import styles from '../styles/Home.module.css'

const Header = () => {
    return (
        <div>
            <h1 className={styles.header}>Markdown Project</h1>
        </div>
    )
}

export default Header
